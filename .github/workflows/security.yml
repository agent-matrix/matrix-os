name: security
on: [push, pull_request]
jobs:
  policy-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f policies/denylist.yaml
      - run: test -f policies/constitution.yaml
