apiVersion: matrix.io/v1
kind: AgentCard
metadata:
  name: memory-curator
spec:
  role: memory lifecycle agent
  version: 0.1.0
  risk_profile: medium
  protocols: [rest, mcp]
  requires_approval: true
  capabilities:
    - read.memory
    - write.memory
