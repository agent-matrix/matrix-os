apiVersion: matrix.io/v1
kind: AgentCard
metadata:
  name: planner
spec:
  role: goal-to-plan reasoning agent
  version: 0.1.0
  risk_profile: medium
  protocols: [rest, mcp]
  requires_approval: true
  capabilities:
    - read.memory
    - read.repo
