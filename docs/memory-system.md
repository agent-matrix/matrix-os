# Memory System

Matrix OS uses Matrix Context as the memory layer.

Memory types: semantic, episodic, procedural, profile, policy, evidence, self.

Every memory event should include source, timestamp, confidence, importance, TTL, privacy label, provenance, and related run ID.

No cross-tenant retrieval. No secret persistence. Inspectability is required for high-impact decisions.
